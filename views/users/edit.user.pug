extends ../layout/common

block content

  .container
    h1 Edit User

    form.d-flex(action='/users/edit-user/'+user.id, method='POST')
      input.form-control.shadow-none(name='userName', type='text', value=user.userName)
      button.btn.btn-primary.ml-2(type='submit') Update